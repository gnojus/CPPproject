#include "packer.h"

namespace Packer {
    using namespace std;

    Packer::Packer(ostream &o) :out (o) {
    }

    void Packer::addFile(string filename) {
    }

    void Packer::create() {
    }
}
